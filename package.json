{
  "name": "autozap-saas",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "dev": "netlify dev",
    "check": "node --check app.js && node --check auth.js && tsc --noEmit",
    "build": "npm run check"
  },
  "devDependencies": {
    "@netlify/functions": "5.3.0",
    "typescript": "5.9.3"
  }
}


